version: 2.1

jobs:
  test:
    docker:
      - image: cimg/python:3.11
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: pip install flask pytest prometheus-flask-exporter elasticsearch
      - run:
          name: Run tests
          command: pytest -v

workflows:
  main:
    jobs:
      - test