version: 2.1

jobs:
  test:
    docker:
      - image: cimg/python:3.11
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: pip install flask pytest prometheus-flask-exporter
      - run:
          name: Run tests
          command: pytest tests/ -v

workflows:
  main:
    jobs:
      - test